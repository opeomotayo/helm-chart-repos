apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nexus-helm-chart2.fullname" . }}-test-connection"
  labels:
    {{- include "nexus-helm-chart2.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nexus-helm-chart2.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
