apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexus-helm-chart.fullname" . }}
  labels:
    {{- include "nexus-helm-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nexus-helm-chart.selectorLabels" . | nindent 4 }}
